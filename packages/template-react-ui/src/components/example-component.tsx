import type { ExampleComponentProps } from "../types";

// Provide a minimal starter component for generated React UI packages.
export function ExampleComponent({ label }: ExampleComponentProps) {
  return <div>{label}</div>;
}
